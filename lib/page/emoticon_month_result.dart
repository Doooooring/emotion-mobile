import "package:flutter/material.dart";

class EmoticonMonthResult extends StatelessWidget {
  const EmoticonMonthResult({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
